 cite-range-separator
 压缩区间的连接符（`[1-3]` 里的那根横线）。
 期望 缺省 "-"（半角连字符）。可给任意字符串；也可给多语言字典按条目语言分设， 未点到的语言退回本参数的缺省值。
 裸标点字符走全 / 半角感知，其余当字面量。


▸ "-"（缺省）

正文[1-3]。

▸ "–"（en dash）

正文[1–3]。

▸ "~"

正文[1~3]。

▸ 多语言字典 (zh: 「～」, rest: 「-」)

正文[1～3]。
